# DriveMind AI — core dependencies (always required)
# Core / data
pandas>=2.1
numpy>=1.26
pyyaml>=6.0
python-dotenv>=1.0
joblib>=1.3

# Traditional ML / NLU
scikit-learn>=1.4

# API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27

# UI
streamlit>=1.32
requests>=2.31

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# Visualization
matplotlib>=3.8
seaborn>=0.13

# ---------------------------------------------------------------------------
# OPTIONAL / HEAVY dependencies live in requirements-ml.txt and
# requirements-llm.txt. The core application (rule-based NLU baseline,
# vehicle simulator, tools, safety layer, agent, API, UI) runs WITHOUT them.
# Install with:
#   pip install -r requirements.txt
#   pip install -r requirements-ml.txt    # transformer model support
#   pip install -r requirements-llm.txt   # OpenAI / LangChain / LangGraph support
# ---------------------------------------------------------------------------
