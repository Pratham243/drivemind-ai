# LLM-backed layer (optional). Without this installed, or without an
# OPENAI_API_KEY set, the system automatically falls back to
# MockLLMProvider and remains fully functional.
#
# langchain/langgraph are deliberately NOT listed here: the agent's
# multi-step planner is a small rule-based conjunction-splitter
# (src/agents/planner.py), not a graph/chain framework — see the
# README's "Architecture decisions & trade-offs" table for why. Adding
# either dependency without importing it anywhere would be exactly the
# "unnecessary framework" anti-pattern this project avoids.
openai>=1.14
