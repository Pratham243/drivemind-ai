# LLM-backed layer (optional). Without these installed, or without an
# OPENAI_API_KEY set, the system automatically falls back to
# MockLLMProvider and remains fully functional.
openai>=1.14
langchain>=0.1
langgraph>=0.0.30
